import { NavLink } from "react-router-dom";

const Logo = () => {
  return (
    <NavLink to="/" className="text-2xl font-bold text-blue-600">
      ShopEase
    </NavLink>
  );
};

export default Logo;